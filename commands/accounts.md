---
description: List the Claude Code accounts managed by cam
allowed-tools: Bash(cam list)
---

Here are the cam-managed accounts (numbered; the active one is marked with `*`):

!`cam list`

Switch with `/claude-account-manager:switch <number|name>` (takes effect on relaunch).
