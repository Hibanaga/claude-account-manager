#!/usr/bin/env node
import('../dist/src/cli.js')
  .then((m) => m.main(process.argv.slice(2)))
  .catch((err) => {
    // cli.main is expected to handle its own errors; this is a last-resort guard.
    console.error(err instanceof Error ? err.message : String(err));
    process.exit(1);
  });
